# ぽいっと！スイーツ — 制作フロー（実行手順書）

> **PLAN.md が「何を作るか」、本書は「どの順番で・どう作るか」。**
> 仕様の詳細は PLAN.md の該当セクションを参照し、本書はタスクの順序・依存関係・完了条件（DoD）を管理する。

---

## 0. 全体マップ

```
Phase 0        Phase 1          Phase 2           Phase 3           Phase 4
準備(1週)  →  プロトタイプ(2週) →  コア完成(3週)  →  polish(3週)   →  リリース準備(2週)
   │             │                 │                 │                 │
   ▼             ▼                 ▼                 ▼                 ▼
[Gate 0]      [Gate 1]          [Gate 2]          [Gate 3]          [Gate 4]
環境が動く     投げて合成できる    1プレイ完走できる   モック並みの見た目   ストア申請可能
```

**合計 11 週間。各 Gate を通過するまで次の Phase に進まない**（アートの先行制作のみ例外、§4 参照）。

### 進行ルール

| ルール | 内容 |
|--------|------|
| 作業単位 | 本書のチェックボックス 1 つ = 1 タスク。終わったらチェックを入れて commit |
| commit 粒度 | 1 タスク 1 commit を基本。メッセージは `feat(phase1): 投げ操作の実装` 形式 |
| ブランチ | 基本 `main` 直。実験的な変更（物理調整の試行等）のみ `try/○○` ブランチ |
| 週次レビュー | 週末に実機（またはエディタ）でプレイ → 気づきを PLAN.md の該当セクションに反映 |
| 仕様変更 | まず PLAN.md を直してから実装する（ドキュメント先行） |

---

## 1. Phase 0 — 準備（1 週間）

**Gate 0: Unity プロジェクトが起動し、空シーンをスマホ実機（または Simulator）で実行できる**

### Day 1–2: 環境構築

- [x] Unity Hub インストール → **Unity 6（6000.2.10f1）** を Android / iOS Build Support 付きでインストール（SDK / NDK / OpenJDK 含む）
- [x] Unity Personal ライセンスを有効化
- [x] プロジェクト新規作成: テンプレート **3D (URP)**、プロジェクト名 `PoittoSweets`、保存先はこのリポジトリ直下
- [x] Blender 最新版をインストール（Phase 2 で使用開始。今は入れるだけ）→ インストール済みを確認

### Day 3: リポジトリ整備

- [x] Unity 用 `.gitignore` を配置（`Library/` `Temp/` `Logs/` `UserSettings/` 等を除外）
- [x] Git LFS を導入し `*.blend` `*.fbx` `*.png` `*.wav` `*.mp3` `*.psd` を LFS 管理に
- [x] `ArtSource/` フォルダ構成を作成（`Sweets/` `Environment/` `Textures/`）
- [x] `PoittoSweets/Assets/` 配下のフォルダ構成を作成（PLAN.md §8.2 の通り）:
  ```
  Assets/{Scenes, Scripts, ScriptableObjects/SweetData, Prefabs/Sweets, Models/Sweets, Models/Environment, Materials, Audio/Temp, UI}
  ```
- [x] ここまでを commit（`chore: プロジェクト初期化`）

### Day 4–5: プロジェクト設定

- [x] Unity メニュー **PoittoSweets → プロジェクト初期セットアップ** を 1 回実行（縦画面固定 / Unity ロゴ非表示 / SweetData 8 ティア分の自動作成。`Assets/Scripts/Editor/ProjectSetup.cs`）
- [ ] タイトルロゴ画像を Splash に設定（ロゴ制作後。Phase 3 でも可）
- [x] パッケージ導入: Input System（テンプレート同梱）/ TextMesh Pro（Unity 6 では uGUI に統合済み）/ ProBuilder 6.1.2（manifest.json に追加済み。次回 Unity 起動時に自動解決）
- [x] `SweetData.cs`（ScriptableObject）の骨格を作成（PLAN.md §8.4 のフィールド定義）
- [x] SweetData アセットを 8 ティア分作成 →「プロジェクト初期セットアップ」メニューが自動作成（初期値: 半径 ×1.3 刻み、スコア倍々 + 最大 1000、ドロップはティア 1〜3 のみ）

### Day 6–7: ビルド確認（Gate 0 判定）

- [x] `Game.unity` シーンを用意（SampleScene をリネーム）
- [x] iPhone 実機でビルド → 起動確認（Xcode 26.6 / 無料 Apple ID 署名）
- [x] **Gate 0 通過（2026-07-28）** — Phase 0 完了

---

## 2. Phase 1 — プロトタイプ（2 週間）

**Gate 1: プレースホルダー（色分け Sphere）だけで「投げて・転がって・合成して・ゲームオーバーになる」**

見た目は一切作り込まない。**すべて Primitive + 単色マテリアル**。

### 実装順序（依存関係順。この順番を守る）

```
① 舞台 → ② 生成 → ③ 操作 → ④ 投げ → ⑤ 合成 → ⑥ ゲームオーバー → ⑦ HUD
```

#### Week 1

- [x] **① 舞台**: カウンター（Cube + Collider）、透明壁 4 面、赤い発射ライン、斜め視点カメラ（「Phase 1 シーンセットアップ」メニューで自動構築）
- [x] **① 物理設定**: 完全 3D + 透明壁 4 面で奥行き制限 に決定（挙動が不安定なら 2.5D 化を再検討）
- [x] **② SweetSpawner**: dropWeight 重み付き抽選（ティア 1〜3）、NEXT 管理、投げ後 0.9 秒で補充
- [x] **③ InputHandler**: ドラッグ左右 = 移動、上下 = 角度 20〜70°（Input System の Pointer でマウス / タッチ両対応）
- [x] **④ 投げ**: リリースで `linearVelocity` 直接設定（角度 + 固定初速 5.2）。方向ガイド線付き

#### Week 2

- [x] **⑤ MergeSystem**: `OnCollisionEnter` で同ティア判定 → 2 個を消して中点に次ティアをポップ生成。`isMerging` フラグで二重合成を防止
- [x] **⑤ スコア**: 合成時に `SweetData.scoreOnMerge` を加算（GameManager に集約）
- [x] **⑥ ゲームオーバー**: 発射ライン（z=-1.8）越え滞留 2 秒で判定（投げ直後 3 秒は猶予）。タップでシーン再読み込み
- [x] **⑦ HUD**: スコア（TMP）+ NEXT 色表示 + GAME OVER 表示
- [x] **Gate 1 通過（2026-07-29）**: iPhone 実機で判定プレイ済み。フィールドはモック準拠の構図に調整完了。スイーツのサイズ・感度は SweetData / Launcher の数値で以降も継続調整する

---

## 3. Phase 2 — コア完成（3 週間）

**Gate 2: タイトル → プレイ → ゲームオーバー → リトライ、を通しで遊べる MVP**

この Phase から **コードとアートの 2 トラック並行**になる。1 日の中で「午前コード / 午後 Blender」のように分けると回しやすい。

### コードトラック

#### Week 3

- [ ] 合成チェーン全 8 ティアの動作確認（ティア 8 到達時の特別処理: 大量スコア + 演出フック）
- [ ] コンボ実装: 短時間の連続合成でボーナス倍率（数値は SweetData / GameManager 側で調整可能に）
- [ ] 弾道プレビュー: 簡易放物線計算で点線 + 着地点円（既存スイーツとの衝突予測はしない。PLAN.md §5.3）

#### Week 4

- [ ] タイトル画面 → ゲーム → リザルト（スコア表示 + リトライ）の画面遷移
- [ ] ポーズメニュー（再開 / リトライ / タイトルへ）
- [ ] SE 仮入れ: フリー素材で「投げ / 着地 / 合成」の 3 音（差し替え前提なので `Audio/Temp/` に置く）

#### Week 5

- [ ] 仮モデルへの差し替え対応: Prefab の見た目だけ入れ替えて物理挙動が変わらないことを確認
- [ ] パラメータ調整期間: dropWeight・スコア・コンボ倍率をエディタ Play 中に触れるようにして調整
- [ ] **Gate 2 判定プレイ**: 通しで 3 プレイ。破綻（すり抜け・二重合成・スコアバグ）ゼロを確認

### アートトラック（Blender 仮モデル）

**スイーツ 1 体の標準制作フロー**（§6 参照）を **ティア 1 → 8 の順に** 回す。Phase 2 では **形状のみ・テクスチャは単色 + 仮の顔** でよい。

- [ ] Week 3: ティア 1〜3（マカロン / チョコシュー / カップケーキ）
- [ ] Week 4: ティア 4〜6（ロールケーキ / ショートケーキ / デコレーションケーキ）
- [ ] Week 5: ティア 7〜8（ホールケーキ / グランドいちごケーキ）+ 全ティアをゲームに組み込み

> 先にティア 1〜3 を作るのは、プレイ中に最も目にする（出現率が高い）ため。ここで作業感覚を掴んでから大型モデルへ。

---

## 4. Phase 3 — ビジュアル polish（3 週間）

**Gate 3: image.png（モックアップ）と並べて「同じゲーム」に見える**

### Week 6: 環境

- [ ] パティスリー背景セット: Asset Store 仮素材で構成 → 足りない什器のみ Blender で制作（PLAN.md §7.4）
- [ ] 奥の大型いちごケーキ（T08 の拡大版、当たり判定なし）
- [ ] 大理石テクスチャの本番化

### Week 7: スイーツ最終化

- [ ] `Face_Atlas.png`（共通顔テクスチャ）を制作 — **全ティアの顔をこれ 1 枚に統一**
- [ ] 各ティアのテクスチャペイント本番化（つや・焼き色。ポリ数 500〜2,000 tris を維持。PLAN.md §7.3）
- [ ] 待機モーション（軽い揺れ / 瞬き）と着地スカッシュを Unity 側の tween で実装

### Week 8: 演出・音・UI

- [ ] ライティング（暖色 Point Light）+ URP ポストプロセス（Bloom 控えめ）
- [ ] パーティクル: 合成キラキラ / 着地の粉 / 最大ティアのフラッシュ + ファンファーレ
- [ ] BGM・SE 本番差し替え（`Audio/Temp/` を空にする）
- [ ] UI 本番化: ピンク丸ボタン・ベージュ数字（Figma でデザイン → 書き出し → Unity UI）
- [ ] **Gate 3 判定**: 実機スクショとモックを並べて比較。ズレが大きい箇所を 1 週分リストアップして潰す

---

## 5. Phase 4 — モバイル対応・リリース準備（2 週間）

**Gate 4: ストア申請を押せる状態**

### Week 9: 技術対応

- [ ] iOS / Android 両ビルドを通す（署名・Bundle ID・バージョン設定）
- [ ] Safe Area 対応（ノッチ・ホームバー）+ 複数解像度でのレイアウト確認
- [ ] パフォーマンス: Profiler で Draw Call / 物理負荷を確認。スイーツ 50 個超で古い最小ティアを削除する上限処理（PLAN.md §11）
- [ ] ハイスコア保存（まず PlayerPrefs。クラウドは任意）
- [ ] 発熱チェック: 実機で 15 分連続プレイ

### Week 10: ストア準備

- [ ] チュートリアル 1 画面（初回起動時のみ。操作説明のオーバーレイ）
- [ ] （任意）リワード広告の組み込み — 入れる場合は審査要件（プライバシーポリシー等）も同時に
- [ ] アプリアイコン・スクリーンショット・ストア説明文
- [ ] プライバシーポリシーページの用意
- [ ] 最終テストプレイ（家族・友人など第三者 2 人以上に触ってもらい、チュートリアルなしで投げ方が分かるか確認）
- [ ] ストア申請

---

## 6. 【繰り返し用】スイーツ 1 体の標準制作フロー

ティアごとに毎回このチェックリストを回す（PLAN.md §7.2 / §7.3 準拠）。**1 体 = 1 commit**。

```
参照確認 → モデリング → (テクスチャ) → エクスポート → インポート → Prefab 化 → ゲーム内確認
```

- [ ] 1. image.png と PLAN.md §7.3 のティア別メモを確認
- [ ] 2. Blender でモデリング（500〜2,000 tris。原点 = 底面中心、スケール 1BU = 1m）
- [ ] 3. テクスチャ（Phase 2: 単色 + 仮顔 / Phase 3: 本番ペイント + Face_Atlas）
- [ ] 4. FBX エクスポート → `ArtSource/Sweets/Sweet_T0X_名前.blend` を保存、FBX は `PoittoSweets/Assets/Models/Sweets/`
- [ ] 5. Unity Import 設定（Scale 1 / Mesh Compression Medium / Read-Write Off / Generate Colliders Off / URP Lit）
- [ ] 6. Prefab 化: Rigidbody + Sphere Collider + SweetController をアタッチ、SweetData の prefab 欄に紐付け
- [ ] 7. ゲーム内で確認: 転がり・合成・サイズ感（前ティアの 1.2〜1.5 倍）が正しいか

---

## 7. 直近の To-Do（今日から始める場合）

1. **Phase 0 / Day 1**: Unity Hub → Unity 6 LTS インストール（ダウンロード中に Day 3 の `.gitignore` 準備を先行してよい）
2. PLAN.md §13-4 の宿題: **合成チェーン 8 段階の名称・見た目リストを確定** — Blender 着手（Week 3）までに済んでいればよいが、早いほどブレない
3. 週次レビューの曜日を決めてカレンダーに登録

---

*作成日: 2026-07-28 / 基準: PLAN.md（同日版）*
